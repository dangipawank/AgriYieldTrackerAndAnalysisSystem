services:
  - type: web
    name: agri-yield-tracker-and-analysis-system
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app:create_app()"
    envVars:
      - key: SECRET_KEY
        generator: secret
